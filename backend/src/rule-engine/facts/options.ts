/**
 * Bounded option lists for the rule value pickers. Maintained here as fixed lists for now;
 * each of these is expected to become its own managed collection later. Seed data draws
 * its collections/types/tags from these values so the catalog and data stay consistent.
 */
export const PRODUCT_COLLECTIONS: string[] = ['summer', 'outdoor', 'winter', 'essentials'];

export const PRODUCT_TYPES: string[] = ['apparel', 'footwear', 'accessories'];

export const PRODUCT_TAGS: string[] = ['eco', 'new-arrival', 'sale', 'limited'];

export const CUSTOMER_TAGS: string[] = ['vip', 'new', 'wholesale', 'loyalty'];
